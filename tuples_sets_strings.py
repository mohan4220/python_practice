"""
Lists =
Tuples = tuple is also a sequential datatype used to store data of same or different datatypes, tuple is immutable

Set, set is unordered and non linear

"""
# lt = [1, 2, 3, 4, 5, 4, 5, 6, 7, 4]
# # tup = (1, 2, 3, 4, 5, 4, 5, 6, 7, 4)
# tup = (8, 9, 10, 11, 12)

# set1 = set(lt)
# set2 = set(tup)

# # set1.add(23)
# # set2.remove(7)
# # set2.update(set1)

# # print(set1, set2, sep="\n")
# print(set1.isdisjoint(set2))

ration_received = {"sumathi", "angelina joli", "shaik rahim"}
ration_non_received = {"vanaja", "kumari", "sreeleela", "anushka", "sumathi"}

# print(ration_received.intersection(ration_non_received))

lt = [1, 2, 3, 4, 5, 6, 8, 8, 9, 9, 0, 0]
lt = set(lt)
lt = list(lt)

# print(lt)

"""
strings, same like list, immutable like tuple, subscriptable, slicable
"""

name = "Apple cider vinegar is good for weight loss and is bad for mouth"

# nums = input("enter nums space seperated: ")
# nums = nums.split()

# for index, num in enumerate(nums):
#     nums[index] = int(num)

print(name.count("is"))


"""
tuples 
sets
strings
"""
